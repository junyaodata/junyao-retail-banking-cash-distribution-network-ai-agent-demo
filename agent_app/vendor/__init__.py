# -*- coding: utf-8 -*-

"""
Third-party code copied into this repo instead of installed as a dependency.

Everything in here was written by someone else and is kept deliberately close
to its original form, so that it can be re-synced against upstream by reading a
diff. Do not grow project logic in this package -- when you need behaviour of
your own, build it in a normal ``agent_app`` module on top of what lives here.
"""
